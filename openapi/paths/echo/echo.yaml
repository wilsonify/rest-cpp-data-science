post:
  tags: [echo]
  operationid: echo_control
  x-openapi-router-controller: openapi_server.controllers.echo_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "echo-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "echo-output.yaml"
