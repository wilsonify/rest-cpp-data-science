post:
  tags: [stats]
  operationid: mean_control
  x-openapi-router-controller: openapi_server.controllers.stats_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "mean-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "mean-output.yaml"
