post:
  tags: [machine_learning]
  operationid: precision_control
  x-openapi-router-controller: openapi_server.controllers.machine_learning_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "precision-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "precision-output.yaml"
