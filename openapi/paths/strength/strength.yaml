post:
  tags: [strength]
  operationid: strengthen_control
  x-openapi-router-controller: openapi_server.controllers.strength_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "strength-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "strength-output.yaml"
