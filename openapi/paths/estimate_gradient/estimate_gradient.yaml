post:
  tags: [gradient_descent]
  operationid: estimate_gradient_control
  x-openapi-router-controller: openapi_server.controllers.gradient_descent_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "estimate_gradient-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "estimate_gradient-output.yaml"
