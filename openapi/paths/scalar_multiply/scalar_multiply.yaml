post:
  tags: [linear_algebra]
  operationid: scalar_multiply_control
  x-openapi-router-controller: openapi_server.controllers.linear_algebra_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "scalar_multiply-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "scalar_multiply-output.yaml"
