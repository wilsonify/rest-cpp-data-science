post:
  tags: [linear_algebra]
  operationid: squared_distance_control
  x-openapi-router-controller: openapi_server.controllers.linear_algebra_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "squared_distance-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "squared_distance-output.yaml"
