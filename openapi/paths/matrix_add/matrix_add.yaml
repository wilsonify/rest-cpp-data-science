post:
  tags: [linear_algebra]
  operationId: matrix_add
  x-openapi-router-controller: openapi_server.controllers.linear_algebra_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "matrix_add-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "matrix_add-output.yaml"
