post:
  tags: [stats]
  operationId: data_range_control
  x-openapi-router-controller: openapi_server.controllers.stats_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "data_range-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "data_range-output.yaml"
